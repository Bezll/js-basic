// === equality operator
// !== not equal operator
// < / >  less / more than operator
// <= / >= less / more than or equal to


let age = 6

if (age <= 7) {
    console.log('Entrance is free of charge')
}

if (age >= 65) {
    console.log('Tickets are £12')
}
